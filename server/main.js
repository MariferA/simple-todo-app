import { Meteor } from 'meteor/meteor';
import "../imports/startup/server";
import '../imports/api/tasks/tasks.resolvers';
import '../imports/api/tasks/tasks.publications';

/**
 * This is the server-side entry point
 */
Meteor.startup(() => {
});
